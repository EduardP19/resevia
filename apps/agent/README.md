# Agent Workspace

This folder is reserved for the agent-side application inside the `resevia` platform layout.

For now, the live agent implementation still exists outside this repo split. When the migration
starts, agent runtime code should land here rather than being mixed into `apps/web`.
